There should be a file called .shell_init in the same directory.

Each commands in the file should be arranged vertically.
For example
"set $KOME =kome
echo $KOME
ls
ls -s
pwd
..."

This can be run as many times even though the file .shell_init changes.

make file is called ...

